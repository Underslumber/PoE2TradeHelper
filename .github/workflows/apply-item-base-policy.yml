name: Apply item base policy

on:
  push:
    branches:
      - codex/item-base-refresh-policy

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-24.04
    env:
      USER_AGENT: poe2-trade-helper-ci/0.1 (contact: github-actions)
      MARKET_SNAPSHOT_ENABLED: false
      FUNPAY_RUB_SNAPSHOT_ENABLED: false
    steps:
      - uses: actions/checkout@v4
      - name: Prepare
        run: |
          echo "Workflow placeholder. Do not merge until source files are changed."
          exit 1
