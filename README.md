# PoE2 Trade Helper

## Цель

Сделать локального помощника для торговли в Path of Exile 2, который помогает быстро находить потенциально выгодные сделки, обмены и предметы для перепродажи.

## Что уже есть

- MCP-слой в `mcp_server.py` для базовой работы с PoE2 `trade2`: лиги, static data, search, fetch и exchange.
- Новый FastAPI-интерфейс в `app/` поверх отдельного торгового слоя `app/trade2.py`.
- Единый источник версии в `app/version.py`; версия выводится под названием приложения в верхней панели UI и используется для cache-busting статических файлов.
- Live UI на `/`, который загружает актуальные лиги и категории из `trade2/data/*`, показывает список позиций и умеет обновлять цены выбранной категории; выбор лиги вынесен в верхнюю панель, а рыночные фильтры показываются только в торговых/аналитических вкладках.
- Для поддерживаемых категорий `get_category_rates` сначала использует прямой JSON endpoint poe.ninja, затем откатывается на `trade2/exchange`.
- Нормализация static entries, exchange listings и poe.ninja overview в единый формат строк.
- Вкладка сигналов показывает рыночную сводку по poe.ninja: падения/росты за 7 дней, объем и риск низкой ликвидности.
- 7-дневные `sparkline.data` из poe.ninja exchange overview трактуются как процентный ряд и перед показом переводятся в ряд цен, привязанный к текущей цене позиции.
- В рыночной сводке есть скоринг лучших сделок: действие, оценка приоритета, риск, объем и позиция цены в 7-дневном диапазоне.
- Торговые сигналы дополнительно проходят проверку практической исполнимости: spread, свежесть снимка, объем/число лотов, риск тонкого рынка и возможного price fixing.
- Сигналы используют внутреннюю историю снимков для сравнения с прошлым снимком без показа raw-лога на странице.
- Для фрагментов и составных предметов есть первый recipe-анализ: backend отдельно считает вход, выход, маржу, покрытие данных и список недостающих цен.
- Поиск публичных priced-лотов конкретного продавца через `trade2/search/fetch` и сравнение с похожими buyout-лотами рынка по типу предмета, уровню, официальным `extended.hashes`/stat id и нормализованным аффиксам.
- Stackable-лоты продавца из поддерживаемых категорий оцениваются быстрее через агрегированную цену poe.ninja; редкие/уникальные предметы остаются на сравнении похожих лотов через `trade2`.
- Кэш снимка публичных лотов продавца, чтобы повторные фильтры и сравнения не выгружали весь список заново.
- В UI можно вставить текст предмета из игры и получить разобранные поля: редкость, название, тип базы, item level, список модов и нормализованные аффиксы; отдельная кнопка запускает осторожную рыночную оценку по похожим публичным priced `trade2` listings.
- Локальная регистрация и авторизация с подтверждением email: пользователи, сессии, закрепленные позиции и журнал сделок хранятся в SQLite.
- Есть отдельная вкладка админки: админ видит список учеток, может выдавать доступ к админке/ИИ и смотреть локальные метрики ИИ-ресурса.
- Подтверждение email умеет отправлять письмо через SMTP из `.env`; если SMTP не настроен, локальный UI показывает dev-ссылку подтверждения.
- Личный кабинет на live UI разделяет отслеживание с графиком, открытые сделки и закрытые сделки: профильная валюта расчета применяется к новым закреплениям/входам, вход фиксирует цену и benchmark, открытая сделка показывает текущую номинальную и реальную маржу, выход сохраняет итоговую маржу.
- Реальная маржа считается относительно выбранного эталона покупательной способности, сейчас это Divine/Exalted/Chaos из валютного рынка. Формула: рост позиции делится на рост benchmark за тот же период, чтобы видеть не только прибыль в валюте, но и изменение покупательной способности.
- В профиле зарегистрированного пользователя можно включить скрытый рублевый слой FunPay: backend парсит только публичную витрину `https://funpay.com/chips/209/`, сохраняет снимки в SQLite, строит почасовой график низкого рынка, показывает рублевые ориентиры валюты и рассчитывает предпочтительный день недели/часовые окна для покупки и продажи Divine за рубли.
- В кабинете можно создать Telegram-уведомления по закрепленным позициям: цена выше/ниже порога, изменение в процентах или любое изменение цены; отправка срабатывает при обновлении цен категории.
- Telegram-уведомления проверяются фоновым worker'ом после новых market snapshots, поэтому события не зависят от открытого браузера.
- В личном кабинете есть короткая памятка по поведению рынка на старте лиги: фазы свежей экономики, риски ранних цен, ликвидность и benchmark-подход.
- Журнал сделок хранит комиссию, валюту комиссии, тег стратегии и причины входа/выхода; закрытые сделки можно экспортировать в CSV.
- Черновой инструмент для ИИ собирает JSON-контекст рынка через `app.ai_context`, API `/api/ai/market-context` и CLI `python -m app.cli ai-context`.
- У пользователя с правом `can_use_ai` появляется вкладка ИИ в live UI: она запускает backend-задачу `/api/ai/market-analysis`, показывает статус, сводку, сигналы, недостающие данные и путь к сохраненному аудиту.
- Веб-API ИИ требует входа в учетку с правом `can_use_ai` или админкой; вызовы пишутся в локальные метрики и ограничиваются `AI_DAILY_QUOTA`. CLI остается локальным инструментом.
- Codex CLI-анализатор рынка запускается через `python -m app.cli market-analyze`: он берет тот же JSON-контекст, вызывает `codex exec` в read-only режиме, валидирует JSON-ответ и сохраняет входной payload/ответ/итоговую оценку в `data/ai_market_analyses`.
- Вкладка ИИ показывает историю прошлых анализов из `data/ai_market_analyses`, чтобы можно было вернуться к старым сигналам без повторного запуска Codex.
- Валютный анализатор `app.currency_analyzer` / `/api/trade/currency-analysis` / `python -m app.cli currency-analyze` строит историю выбранной валюты, локальный тренд, волатильность и осторожный прогноз; из вкладки ИИ можно передать этот контекст в Codex через `/api/ai/currency-analysis` и увидеть отдельный совет.
- Кросс-валютная вкладка ищет настоящие циклы обмена `A -> B -> ... -> A` по направленным online `trade2/exchange` офферам, применяет зазор на каждый шаг, ранжирует короткие выгодные маршруты и показывает отдельные шаги сделки.
- Фоновый CLI-сборщик `python -m app.cli market-snapshots` делает регулярные слепки всех stackable-категорий актуальной лиги в `status=any`: 5 минут в первые 48 часов при заданном `--league-start`, затем 15 минут.
- При запуске FastAPI-приложения фоновый сервис слепков включен по умолчанию: он выбирает актуальную PoE2 trade-лигу, собирает market snapshots и раз в 10 минут проверяет появление новой лиги.
- Live UI берет последний сохраненный снимок из SQLite и автообновляет открытую категорию, когда в базе появляется более новый `created_ts`; ручная кнопка обновления остается принудительным live-запросом.
- История `market_history` поддерживает `raw`/`hourly`/`daily` granularity: старые raw-снимки можно сжимать в почасовые агрегаты, а старые почасовые - в дневные.
- Тем же фоновым циклом собирается рублевая база FunPay для Divine: один публичный снимок `chips/209` на цикл, сохранение в `funpay_rub_snapshots` / `funpay_rub_offers`, настройка через `FUNPAY_RUB_SNAPSHOT_ENABLED` и `FUNPAY_RUB_SNAPSHOT_TARGET`.
- Добавлен GitHub Actions CI/CD: проверки запускаются на `push`/PR в `main`, а деплой по SSH включается отдельной переменной после настройки серверных секретов. Подробности в `docs/deployment.md`.
- Для графиков одной позиции есть легкий API `/api/trade/history/item`, чтобы читать длинный ряд цены или спроса без отправки всех строк всех слепков в браузер.
- Расчет Liquid Emotions перебирает весь путь преобразований `3^k x эмоция уровня N -> эмоция уровня N+k`, чтобы находить лучшие точки входа и выхода, а затем скрывает продолжения, где более короткий выход дает не худшее качество сигнала и лучшую маржу.
- Внутренняя история снимков live-курсов в JSONL для анализа и API, без отдельного видимого лога на странице.
- Старый collector для poe.ninja с discovery/sync, SQLite-хранилищем, иконками, CSV/JSONL export и страницей `/economy`.
- Набор быстрых unit-тестов для нормализации trade2/poe.ninja данных, парсера строк и placeholder-иконок.

## Что не доделано

- Модель прибыльности остается осторожной: она уже учитывает spread, свежесть, объем и риски listings, но это не гарантия фактического исполнения сделки.
- Для фрагментов, проходок и boss-entry предметов есть первый recipe-framework, но каталог рецептов пока неполный; Delirium уже покрывает Симулякр и полные 3-в-1 цепочки жидких эмоций.
- Старый `data/trade_rate_history.jsonl` остается legacy/fallback источником; основная история и compaction живут в SQLite.
- Анализ вставленного текста предмета из игры пока умеет parser/normalizer и осторожную оценку по похожим listings; для точной редкой экипировки все еще нужна ручная проверка похожести и будущая более строгая модель official stat id.
- Анализ выставленных игроком товаров не реализован; для PoE2 пока нет подтвержденного официального пути к личным stash данным.
- Поиск лотов продавца работает только по публичным trade2 listings и не заменяет доступ к личным stash данным.
- README пока не описывает пользовательский сценарий запуска и ограничения UI достаточно подробно.
- Рублевый слой FunPay считает изменения запасов и числа лотов только как прокси спроса/предложения: это не подтвержденные сделки и не стакан исполненных покупок. Завышенные цены отсекаются из ориентира по низу рынка, но фейковые дешевые listings все еще требуют ручной проверки.

`mcp_server.py` логирует внешние вызовы без тел запросов и секретов. Ошибки API возвращаются в виде словаря с `error` и реальным `status_code`, если сервер его прислал.

OAuth-токен Path of Exile нужен, если используем официальный API `https://api.pathofexile.com`, например:

## Правило ведения плана

- Выполненные пункты не удалять, а помечать галкой: `[x]`.
- Пункты в работе помечать гаечным ключом: `🔧`.
- Новые пункты добавлять с пометкой `новое`.

## Ближайший план

- [x] Выбор актуальной PoE2-лиги и работа с ее рыночными данными.
- [x] Сбор текущих цен по валютам, фрагментам, эмоциям, эссенциям, рунам и другим полезным категориям.
- [x] Нормализация цен из разных источников, чтобы можно было сравнивать позиции между собой.
- [x] История снимков цен, чтобы видеть изменение рынка во времени, а не только лог последних запросов.
- [x] 🔧 Поиск обменов с потенциальной прибылью с учетом spread, объема, свежести listings и риска price fixing.
- [x] 🔧 Анализ 3-в-1 цепочек, например Liquid Emotions и других категорий, где есть апгрейд/обмен.
- [ ] 🔧 Отдельный анализ проходок, фрагментов и boss-entry предметов: recipe-framework есть, Delirium покрыт частично, нужно расширять каталог остальных рецептов.
- [x] 🔧 Фильтр сигналов по практической исполнимости: не только математическая прибыль, но и достаточный объем рынка.
- [x] Удобный локальный интерфейс для просмотра категорий, цен, медиан, объема, изменения за период и найденных сигналов.
- [x] 🔧 Возможность вставить текст предмета из игры и получить оценку цены или торгового потенциала: parser/normalizer и первый поиск похожих public listings готовы.
- [ ] Позже - анализ выставленных игроком товаров, если появится надежный рабочий путь для получения этих данных в PoE2.
- [x] новое: Добавить вкладку поиска публичных лотов продавца с оценкой актуальной цены по похожим buyout-лотам.
- [x] новое: Кэшировать выгруженные лоты продавца и показывать индикатор ожидания во время долгих торговых операций.
- [x] новое: Сравнивать предметы продавца с рынком по типу, уровню и официальным stat id; при малой выборке откатываться к сравнению по аффиксам минус один и показывать уверенность оценки.
- [x] новое: В UI разделить торговые рекомендации на "сигнал", "слабый сигнал" и "только наблюдать".
- [x] новое: При открытии валюты показывать график изменения цены и выбор валюты оценки рядом с графиком.
- [x] новое: Строго разделить русскую и английскую локализации видимого UI-текста.
- [x] новое: Загружать последний сохраненный снимок цен при открытии страницы и добавить настройку автообновления 1м / 5м / 30м / 1ч.
- [x] новое: Явно показывать валюту оценки с названием и иконкой в списке, сводке и деталях позиции.
- [x] новое: Для Liquid Emotions анализировать весь путь улучшений и искать лучшие точки входа и выхода.
- [x] новое: В основном блоке рекомендаций оставлять топ-5, полный список и кросс-валютные варианты выносить в отдельные вкладки.
- [x] новое: Вынести подробные сигналы в отдельную вкладку с разделами "Купить", "Продать", "Лучшие операции" и выбором длины цепочек до 5.
- [x] новое: Брать русские термины из официального `ru.pathofexile.com/api/trade2/data/static` и показывать иконки в карточках сигналов.
- [x] новое: Добавить мини-графики в карточки сигналов с выделением текущей области и подписью основы сигнала.
- [x] новое: Сделать графики в сигналах длиннее и разместить их ближе к тексту предложения.
- [x] новое: Переформатировать сигналы в адаптивную сетку и добавить деления на мини-графики.
- [x] новое: Добавить дневную разбивку и числовые значения на мини-графики сигналов.
- [x] новое: Подписывать дни на мини-графиках конкретными датами в формате `дд.мм`.
- [x] новое: Добавить ценовую шкалу на график детали позиции и переключатель между ценой и спросом.
- [x] новое: Добавить выбор периода графиков 3/7/14/30 дней и растянуть графики кабинета на ширину карточки.
- [x] новое: Добавить таблицу самых активно торгуемых товаров и многоходовые сделки по нескольким валютам оценки.
- [x] новое: Добавить локальную регистрацию, авторизацию, закрепленные позиции и журнал сделок с фиксацией входа/выхода.
- [x] новое: Добавить отдельную вкладку админки, список пользователей, выдачу доступа к админке/ИИ-функциям и локальные метрики ИИ-квоты.
- [x] новое: Требовать подтверждение email при регистрации и поддержать SMTP/dev-ссылку для локального режима.
- [x] новое: Добавить Telegram-уведомления по закрепленным позициям с пользовательским chat id и выбранным типом события.
- [x] новое: Разделить кабинет на отслеживание с графиком, открытые сделки с актуальной маржой и закрытые сделки с итоговой маржой.
- [x] новое: Добавить benchmark-adjusted P/L для учета обесценивания валюты между входом и выходом.
- [x] новое: Добавить памятку игрока по стартовой экономике в личный кабинет.
- [x] новое: Добавить первый инструмент для ИИ: JSON-контекст рынка из последнего снимка или свежего запроса.
- [x] новое: Добавить Codex CLI-анализатор рынка поверх JSON-контекста с сохранением аудита входа и ответа.
- [x] новое: Добавить вкладку ИИ для пользователей с доступом `can_use_ai`, backend-задачу анализа и отображение проверяемых сигналов в UI.
- [x] новое: Добавить валютный анализатор с графиком фактического тренда, графиком прогноза и отдельным ИИ-советом по валютному контексту.
- [x] новое: Добавить CLI для регулярных слепков всего stackable-рынка с режимом 5 минут первые 48 часов и 15 минут дальше.
- [x] новое: Автоматически запускать сбор слепков вместе с приложением и переключаться на новую trade-лигу, когда она появляется в списке лиг.
- [x] новое: Добавить API временного ряда одной позиции для более информативных графиков цены и спроса.
- [x] новое: Добавить скрытый профильный рублевый слой FunPay для оценки валюты относительно ₽ без рублевого пересчета маржи.
- [x] новое: Разделить кабинет на вкладки закрепленных позиций и рублевой аналитики, добавить почасовой график и расчет RUB по низу ликвидного рынка.
- [x] новое: Подключить автоматический сбор рублевой базы FunPay тем же циклом, что и market snapshots, плюс отдельную CLI-команду для ручного запуска.
- [x] новое: Добавить расчет лучшего дня недели и 1-2 часовых интервалов для покупки Divine за ₽ и продажи Divine за ₽ по накопленной RUB-истории.
- [x] новое: Добавить compaction истории: сырые слепки 7 дней, почасовые агрегаты до 30 дней, дневные агрегаты старше 30 дней.
- [ ] 🔧 новое: Доработать журнал сделок до мультивалютного P/L, комиссий, тегов стратегии и заметок по причинам входа/выхода: комиссии, теги и причины готовы, полный мультивалютный P/L еще нужно расширять.
- [x] новое: Добавить составной market-basket benchmark из ликвидных валют/предметов для старта сезона, где Divine может быть слишком редким и волатильным.
- [x] новое: Добавить отдельный фоновый мониторинг уведомлений, чтобы Telegram-события приходили без открытого UI.
- [x] новое: Добавить историю прошлых ИИ-анализов в UI, чтобы пользователь мог вернуться к старым сигналам без повторного запуска Codex.
- [x] новое: Добавить parser/normalizer вставленного текста предмета как базу для будущего item pricing engine.
- [x] новое: Добавить рыночную оценку pasted-item через похожие публичные `trade2` лоты с fallback-сравнением по нормализованному тексту аффиксов.
- [x] новое: Добавить execution-quality прямо в строки рынка и показать количество исполнимых/рискованных позиций в рыночном здоровье.
- [x] новое: Исправить кросс-валюту на настоящий поиск циклов `A -> B -> ... -> A` через `trade2/exchange`, а не сравнение одного товара в разных валютах.

## Запуск

Установка зависимостей в уже созданное локальное окружение:

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

Команда ставит зависимости проекта из `requirements.txt` в `.venv`.

Запуск нового локального UI:

```powershell
python -m app.cli run
```

Команда поднимает FastAPI-приложение на `http://localhost:8000`.
Пока приложение запущено, фоновый сервис market snapshots активен по умолчанию. Его статус доступен через `/api/trade/market-snapshot-service`.
Live UI при открытии и автообновлении читает свежие сохраненные слепки из SQLite; кнопка "Обновить цены категории" делает отдельный live-запрос и также записывает результат в историю.
Если в `.env` задан `ADMIN_PASSWORD`, при старте будет создана или обновлена учетка `ADMIN_USERNAME` с правами админа и ИИ. Если в базе нет админов, первый существующий или первый зарегистрированный пользователь тоже становится админом. Вкладка админки показывается только админам, вкладка ИИ - только админам и пользователям с `can_use_ai`. `AI_DAILY_QUOTA` задает локальную дневную квоту веб-ИИ; `0` отключает локальный лимит.

Одноразовый сбор слепков рынка:

```powershell
python -m app.cli market-snapshots --league "Fate of the Vaal" --once
```

Команда собирает все stackable-категории через poe.ninja/trade2 и пишет историю в SQLite (`data/poe2_ninja.sqlite`, таблица `market_history`). Старый `data/trade_rate_history.jsonl` используется только как источник миграции и fallback для legacy-запусков.

Анализ рынка через Codex CLI:

```powershell
python -m app.cli market-analyze --league "Fate of the Vaal" --category Currency --target exalted --status any --league-day 1
```

Команда берет последний сохраненный снимок категории, строит JSON-контекст для ИИ, запускает `codex exec` в read-only режиме и печатает нормализованную оценку. Результат аудита сохраняется в `data/ai_market_analyses`. Если добавить `--refresh`, перед анализом будет сделан свежий запрос к рынку.

Анализ одной валюты:

```powershell
python -m app.cli currency-analyze --league "Fate of the Vaal" --currency divine --target exalted --status any --league-day 1 --horizon-hours 24
```

Команда читает сохраненные снимки Currency, считает историю, направление тренда, волатильность и локальный прогноз. Если добавить `--with-ai`, тот же контекст будет передан Codex CLI в read-only режиме для текстового совета.

Поиск кросс-валютных циклов:

```powershell
python -m app.cli currency-cycles --league "Fate of the Vaal" --base exalted --targets exalted,divine,chaos --status online --max-steps 4
```

Команда проверяет направленные `trade2/exchange` пары между выбранными валютами, применяет расчетный зазор на каждый шаг и выводит циклы, где финишная сумма базовой валюты выше стартовой.

Непрерывный сбор для старта лиги:

```powershell
python -m app.cli market-snapshots --league "Fate of the Vaal" --league-start "2026-05-16T22:00:00+03:00" --currency-targets divine,chaos
```

Команда использует `status=any`, собирает основной рынок в `exalted`, дополнительно пишет валютные benchmark-снимки в `divine` и `chaos`, а частоту выбирает по окну старта: 5 минут первые 48 часов, 15 минут дальше.

Разовый сбор рублевой базы FunPay:

```powershell
python -m app.cli funpay-rub-snapshots --league "Fate of the Vaal" --once
```

Команда парсит публичную витрину `https://funpay.com/chips/209/`, сохраняет снимок в SQLite и возвращает агрегат по Divine в ₽. Без `--once` работает циклом с теми же параметрами частоты, что `market-snapshots`: 5 минут в раннем окне лиги и 15 минут дальше.

Разовое сжатие старой истории рынка:

```powershell
python -m app.cli compact-history
```

Команда агрегирует старые raw-записи `market_history` в почасовые и дневные строки SQLite по текущей политике хранения.

Ручная проверка Telegram-уведомлений без открытого UI:

```powershell
python -m app.cli notifications-check --league "Fate of the Vaal"
```

Команда берет последние сохраненные снимки рынка, проверяет активные пользовательские правила и отправляет подходящие Telegram-события.

Разбор вставленного текста предмета:

```powershell
Get-Content .\item.txt -Raw | python -m app.cli parse-item-text
```

Команда читает текст предмета из stdin, нормализует поля и печатает JSON с редкостью, названием, типом базы, item level и модами.

Оценка вставленного текста предмета по похожим публичным listings:

```powershell
Get-Content .\item.txt -Raw | python -m app.cli price-item-text --league "Fate of the Vaal" --target exalted --status any
```

Команда сначала разбирает pasted-текст, затем ищет похожие priced-лоты через `trade2/search/fetch`, считает медиану и диапазон в выбранной валюте. Для редких предметов это ориентир, а не точная цена.

Быстрая проверка Python-кода:

```powershell
python -m compileall mcp_server.py app
python -m pytest -q
```

Первая команда рекурсивно проверяет синтаксис всех Python-файлов проекта без запуска приложения. Вторая запускает unit-тесты.
